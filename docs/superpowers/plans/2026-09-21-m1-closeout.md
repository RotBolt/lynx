# Lynx M1 Closeout Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the five remaining M1 gaps so an agent can capture HTTP/1.1, HTTP/2, and WebSocket exchanges, use stable lifecycle/proxy state, and verify the complete Android workflow through structured JSON.

**Architecture:** Keep Lynx-owned `NetworkExchange` models and put protocol handling behind a `ProxyEngine` boundary. The current HTTP/1 parser becomes the compatibility adapter while protocol-specific handlers normalize headers, body bytes, decompression, failures, and frame events into the same evidence store. Android proxy changes remain transactional and session-owned.

**Tech Stack:** Kotlin/JVM 21, Java sockets/SSLEngine, Bouncy Castle, kotlinx-coroutines, JUnit/Kotlin test, ADB.

**Spec:** `lynx-spec/MILESTONES.md`, `lynx-spec/IMPLEMENTATION_TICKETS.md`, `lynx-spec/ARCHITECTURE.md`.

## Global Constraints

- Debuggable Android applications only.
- ADB and the injected/runtime-independent host proxy are the attachment mechanisms.
- macOS is the first host platform.
- Database operations remain read-only.
- No VPN/TUN service and no application code changes.
- Unsupported/bypassed traffic must be reported explicitly.
- Finite responses are versioned JSON; watch output is JSONL.

### Task 1: Proxy protocol boundary and response normalization

**Files:** `host/network/src/main/kotlin/dev/lynx/network/HttpProxyCapture.kt`, `host/network/src/main/kotlin/dev/lynx/network/ProxyEngine.kt`, `host/network/src/test/kotlin/dev/lynx/network/HttpProxyCaptureTest.kt`

- [ ] Extract protocol-neutral exchange recording and body normalization.
- [ ] Add tests for gzip response reframing, decoded capture bodies, and failure retention.
- [ ] Run `./gradlew :host:network:test`.

### Task 2: HTTP/2 CONNECT capture

**Files:** `host/network/src/main/kotlin/dev/lynx/network/Http2ProxyHandler.kt`, `host/network/src/main/kotlin/dev/lynx/network/HttpProxyCapture.kt`, `host/network/src/test/kotlin/dev/lynx/network/Http2ProxyHandlerTest.kt`

- [ ] Negotiate ALPN `h2` on MITM TLS sockets.
- [ ] Decode SETTINGS, HEADERS, DATA, RST_STREAM, and GOAWAY frames.
- [ ] Forward streams upstream and record complete request/response headers and bodies.
- [ ] Return an explicit `HTTP2_UNSUPPORTED` failure for malformed/unsupported frames.
- [ ] Add an in-process TLS HTTP/2 integration test and run network tests.

### Task 3: WebSocket capture

**Files:** `host/network/src/main/kotlin/dev/lynx/network/WebSocketProxyHandler.kt`, `host/network/src/main/kotlin/dev/lynx/network/HttpProxyCapture.kt`, `core/model/src/main/kotlin/dev/lynx/model/NetworkModels.kt`, tests

- [ ] Detect HTTP/1.1 `Upgrade: websocket` and preserve the 101 response.
- [ ] Relay and record text, binary, close, ping, and pong frames.
- [ ] Preserve masking rules and report malformed frames without silently dropping them.
- [ ] Add a local client/server frame integration test and JSON serialization coverage.

### Task 4: Android proxy and trust lifecycle hardening

**Files:** `host/network/src/main/kotlin/dev/lynx/network/AndroidProxyController.kt`, `host/daemon/src/main/kotlin/dev/lynx/daemon/DaemonService.kt`, installer tests/docs

- [ ] Make start apply the endpoint and verify it before reporting started.
- [ ] Make stop/detach restore the exact prior proxy state idempotently.
- [ ] Distinguish configured, reachable, restored, bypassed, and CA-installed states.
- [ ] Keep Android CA staging/install confirmation explicit; complete simulator/device guidance.
- [ ] Add rollback and repeated-start/stop tests.

### Task 5: M1 release gate and agent contract

**Files:** `apps/cli`, `lynx-spec/IMPLEMENTATION_TICKETS.md`, `lynx-spec/MILESTONES.md`, release smoke script/docs

- [ ] Verify `network list` and `network get` return complete structured HTTP/HTTPS/HTTP2/WebSocket evidence.
- [ ] Verify attach, process restart rebinding, database snapshot/query, detach invalidation, and proxy restoration.
- [ ] Record exact macOS emulator commands and outputs.
- [ ] Mark only evidence-backed tickets complete.

